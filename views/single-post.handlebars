<div class="row">
    <div class="card col-8 mb-3 mx-auto">
    <div class="card-body">
        <div class="card-title">
            {{!-- have href link to individual post pages --}}
            <a href="/post/{{post.id}}">{{post.title}}</a>
        </div>
        <div class="card-text">
            <p>{{post.body}}</p>
            <a class="blockquote-footer">by {{post.user.username}} on
            {{format_date post.created_at}}</a>
        </div>
        <div class="footer"> 
            {{post.vote_count}} {{format_plural "like" post.vote_count}}
            |
            <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
            {{#if loggedIn}}
            <textarea name="comment-body" class="col-12"></textarea>
            <button id="like-button" class="btn btn-primary">like</button>
            <button id="comment-button" class="btn btn-primary">comment</button>
            {{/if}}
        </div>
    </div>
</div>
{{!-- {{#if loggedIn}} --}}
    {{!-- <div class="card col-7 mb-3 text-left">
        <div class="card-title">
            New Comment
        </div>
        <div class="card-body">
            <textarea name="comment-body"></textarea>
            <button type="submit" class="btn bg-primary text-light">add comment</button>
        </div>
    </div> --}}
    {{!-- {{/if}} --}}
<div class="mx-auto col-9">
{{> comments post.comments }}
</div>