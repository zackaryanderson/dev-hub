<div class="row">
    <div class="card col-11 col-md-9 mb-3 mx-auto small-margin">
        <div class="card-body">
            <div class="card-title">
                {{!-- have href link to individual post pages --}}
                <a href="/post/{{post.id}}">{{post.title}}</a>
            </div>
            <div class="card-text">
                <p>{{post.body}}</p>
                <p class="blockquote-footer">by <a href="/userpage/{{post.user.id}}">{{post.user.username}}</a> on
                    {{format_date post.created_at}}</p>
            </div>
            <div class="footer">
                {{post.vote_count}} {{format_plural "like" post.vote_count}}
                |
                <a href="/post/{{post.id}}">{{post.comments.length}}
                    {{format_plural "comment" post.comments.length}}</a>
                {{#if loggedIn}}
                <textarea name="comment-body" class="col-12"></textarea>
                <button class="btn upvote-btn"><span class="oi oi-star"></span> Like</button>
                <button id="comment-button" class="btn"><span class="oi oi-code"></span> Comment</button>
                {{else}}
                <h6 class="footer" style="color:grey; margin-top: 1vh">login to like and comment</h6>
                {{/if}}
            </div>
        </div>
    </div>
</div>
<div class="mx-auto col-12 col-md-8 mb-3">
{{> comments post.comments }}
</div>

<script src="/javascript/comment.js"></script>
<script src="/javascript/upvote.js"></script>